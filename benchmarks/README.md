# Benchmarks & Leaderboard

This directory contains benchmark datasets, evaluation harnesses, and baseline golden metrics.

## Submitting to the Leaderboard
To submit your model predictions:
1. Follow the data split protocol (patient-level test splits).
2. Generate prediction masks/outputs according to the standardized format.
3. Open a Pull Request with your predictions under `benchmarks/submissions/<your_team>/`.
